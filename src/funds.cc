//
// Created by GWC-CHC-07 on 7/6/2020.
//

#include "Card/funds.h"

namespace monopoly {
  void Funds::PerformAction(const std::vector<Tile *> &tiles,
                            int player_id) const {
  
    if (action_ == CardAction::kAddFunds) {
    
    
    } else if (action_ == CardAction::kAddFundsToPlayers) {
    
    
    } else {
    }
  }
}